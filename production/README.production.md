# OSCAR BROOME REVENUE - PRODUCTION DEPLOYMENT GUIDE\n\n## 🚀 QUICK START OPTIONS\n\n### Option 1: Local Docker (Full Stack - RECOMMENDED)\n```\n# 1. Copy env template\ncp production/.env.production.example .env.production\n# Edit .env.production with your secrets\n\n# 2. Deploy full stack\ndocker compose -f docker-compose.production.yml up -d\n\n# 3. Check status\n docker compose -f docker-compose.production.yml ps\n curl http://localhost:80/health\n```\n\nGrafana: http://localhost:3001 (admin/admin)\nPrometheus: http://localhost:9090\nApp: http://localhost (nginx proxy)\n\n### Option 2: Local PM2 (Node Only - Fast Test)\n```\nnpm install\ncp production/.env.production.example .env.production\n# Edit secrets, set SKIP_DATABASE=true for quick test\nnpm run prod  # or node production_deploy.js\npm2 monit\n```\n\n### Option 3: Unified Script\n```\nbash production/deploy-prod.sh local-docker\n# or local-pm2\n```\n\n## 🔧 REQUIRED SECRETS (Edit .env.production)\n\n| Service | Env Var | Example |\n|---------|---------|---------|\n| Mongo | MONGODB_PASSWORD | supersecret123 |\n| Redis | REDIS_PASSWORD | redis123 |\n| Stripe | STRIPE_SECRET_KEY | sk_live_... |\n| Twilio | TWILIO_ACCOUNT_SID | ACxx... |\n| SendGrid | SENDGRID_API_KEY | SG.xxx |\n| JPM | JPMORGAN_CLIENT_ID | your_client_id |\n\nFull template: `production/.env.production.example`\n\n## 📊 MONITORING\n- Health: http://localhost/health\n- Metrics: http://localhost/metrics \n- Logs: `docker compose logs -f app` or `pm2 logs`\n\n## 🌐 CLOUD DEPLOY (AWS/VPS)\n1. Copy project to EC2/VPS\n2. `bash production/deploy-prod.sh local-docker`\n3. Setup firewall (80/443/3000)\n4. SSL: Add certs to ssl/ dir for nginx\n5. Domain DNS to server IP\n\n## ✅ PRODUCTION FEATURES ACTIVE\n- Merchant/Stripe payments (/api/merchant)\n- JPMorgan treasury (/jpmorgan)\n- UBI Heaven on Earth (/api/ubi)\n- King Sachem ITG (/api/itg)\n- Divine AI (/api/divine-ai)\n- Blackbox Multi-Agent (/api/multi-agent)\n- Full monitoring stack\n\n## 🛡️ SSL SETUP\n```\nmkdir -p ssl\n# Add yourdomain.com.crt, yourdomain.com.key to ssl/\n# Restart nginx container\n```\n\n## 📈 SCALE\n`docker compose up -d --scale app=3`\n\n**Production version complete. Revenue empire ready.**

# Oscar Broome Revenue Override Capabilities

## Overview

This enhanced Oscar Broome Revenue System includes comprehensive transaction override capabilities, allowing authorized users to modify, approve, or reject transaction changes with full audit logging.

## Features

### Transaction Override System

- **Override Request Creation**: Submit override requests for any transaction
- **Approval Workflow**: Multi-level approval process with role-based access
- **Audit Trail**: Complete audit logging for all override operations
- **Real-time Monitoring**: Dashboard for managing override requests
- **Security**: Enhanced authentication and authorization

### User Roles

- **Admin**: Full override capabilities and system management
- **Override Manager**: Create and manage override requests
- **Super Admin**: Complete system access and override approval

### API Endpoints

#### Transaction Override Endpoints

- `GET /api/transactions/overrides` - List all override requests
- `POST /api/transactions/override` - Create new override request
- `PUT /api/transactions/:id/override` - Update transaction with override
- `DELETE /api/transactions/:id/override` - Reject override request
- `GET /api/transactions/:id/audit` - Get transaction audit trail

### System Endpoints

- `GET /api/earnings` - Get earnings data
- `GET /health` - System health check
- `GET /override-dashboard` - Override management dashboard

## Installation

1. **Install Dependencies**

   ```bash
   npm install
   ```

2. **Environment Configuration**
   Create `.env` file:

   ```
   PORT=4000
   ADMIN_USER=admin
   ADMIN_PASS=securepassword
   OVERRIDE_MANAGER_USER=override_manager
   OVERRIDE_MANAGER_PASS=override123
   CORS_ORIGIN=http://localhost:3000
   REVENUE_DATA_PATH=./owlban_repos/aggregated_revenue.json
   ```

3. **Start the Server**

   ```bash
   npm start
   ```

## Usage

1. **Accessing the Override Dashboard**
   1. Navigate to `http://localhost:4000/override-dashboard`
   2. Login with credentials:
      - Username: `admin`
      - Password: `securepassword`

2. **Accessing the Override Dashboard**
   1. Navigate to `http://localhost:4000/override-dashboard`
   2. Login with credentials:
      - Username: `admin`
      - Password: `securepassword`

3. **Accessing the Override Dashboard**
   1. Navigate to `http://localhost:4000/override-dashboard`
   2. Login with credentials:
      - Username: `admin`
      - Password: `securepassword`

4. **Accessing the Override Dashboard**
   1. Navigate to `http://localhost:4000/override-dashboard`
   2. Login with credentials:
      - Username: `admin`
      - Password: `securepassword`

5. **Accessing the Override Dashboard**
   1. Navigate to `http://localhost:4000/override-dashboard`
   2. Login with credentials:
      - Username: `admin`
      - Password: `securepassword`

6. **Accessing the Override Dashboard**
   1. Navigate to `http://localhost:4000/override-dashboard`
   2. Login with credentials:
      - Username: `admin`
      - Password: `securepassword`

7. **Accessing the Override Dashboard**
   1. Navigate to `http://localhost:4000/override-dashboard`
   2. Login with credentials:
      - Username: `admin`
      - Password: `securepassword`

8. **Accessing the Override Dashboard**
   1. Navigate to `http://localhost:4000/override-dashboard`
   2. Login with credentials:
      - Username: `admin`
      - Password: `securepassword`

9. **Accessing the Override Dashboard**
   1. Navigate to `http://localhost:4000/override-dashboard`
   2. Login with credentials:
      - Username: `admin`
      - Password: `securepassword`

10. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword`

11. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword`

12. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword`

13. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword`

14. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword`

15. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword`

16. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword`

17. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword`

18. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword`

19. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword`

20. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword`

21. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword`

22. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword`

23. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword`

24. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword`

25. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword`

26. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword`

27. **Accessing the Override Dashboard**
    1. Navigate to `http://localhost:4000/override-dashboard`
    2. Login with credentials:
       - Username: `admin`
       - Password: `securepassword"
